Butcher Backend (TypeScript) - quick start
1. Copy .env.example to .env and set DATABASE_URL and AWS keys
2. npm install
3. Run migrations (use psql or run migrate script if implemented)
4. npm run seed (to insert demo org and admin)
5. npm run dev
